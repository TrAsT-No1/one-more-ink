---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/providers/lenis-provider.tsx
  - src/components/animations/fade-in.tsx
  - src/components/animations/slide-up.tsx
  - src/components/animations/stagger-container.tsx
  - src/lib/animations.ts
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Lo smooth scroll Lenis e attivo — lo scroll della pagina ha un'inerzia morbida, diversa dallo scroll nativo"
    - "I componenti FadeIn, SlideUp, StaggerContainer animano gli elementi quando entrano nel viewport"
    - "Le animazioni non causano jank visibile con CPU throttling 4x in Chrome DevTools"
    - "Lenis non interferisce con anchor links (#sezione) ne con il back button del browser"
    - "Il bundle Motion usa LazyMotion+m (~5kb), NON motion.div (34kb)"
  artifacts:
    - path: "src/components/providers/lenis-provider.tsx"
      provides: "Lenis smooth scroll con sync GSAP ticker"
      exports: ["LenisProvider", "useLenis"]
    - path: "src/components/animations/fade-in.tsx"
      provides: "Wrapper FadeIn con direzione configurabile"
      exports: ["FadeIn"]
    - path: "src/components/animations/slide-up.tsx"
      provides: "Wrapper SlideUp per reveal dal basso"
      exports: ["SlideUp"]
    - path: "src/components/animations/stagger-container.tsx"
      provides: "Container che stagger-anima i figli"
      exports: ["StaggerContainer"]
    - path: "src/lib/animations.ts"
      provides: "Varianti condivise, easing, durate"
      exports: ["EASE_SMOOTH", "EASE_OUT_EXPO", "DURATION", "staggerContainer", "fadeInUp", "fadeIn", "scaleIn"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/lenis-provider.tsx"
      via: "Provider wrapper"
      pattern: "LenisProvider"
    - from: "src/app/layout.tsx"
      to: "motion/react"
      via: "LazyMotion wrapper"
      pattern: "LazyMotion.*domAnimation"
    - from: "src/components/providers/lenis-provider.tsx"
      to: "gsap"
      via: "Ticker sync (autoRaf: false)"
      pattern: "autoRaf.*false"
    - from: "src/components/animations/fade-in.tsx"
      to: "src/lib/animations.ts"
      via: "Shared easing/duration"
      pattern: "EASE_SMOOTH|DURATION"
---

<objective>
Animation primitives riutilizzabili (FadeIn, SlideUp, StaggerContainer), Lenis smooth scroll sincronizzato con GSAP, e lib/animations.ts con varianti condivise. Pagina di test aggiornata per dimostrare tutte le animazioni.

Purpose: Creare i building blocks di animazione che ogni sezione del sito usera. L'investimento qui previene codice duplicato e garantisce performance consistente.
Output: 3 componenti animazione, 1 provider Lenis, 1 file varianti, layout aggiornato, pagina demo scrollabile.
</objective>

<execution_context>
@/Users/federicobavieri/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicobavieri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LenisProvider, LazyMotion wrapper in layout, lib/animations.ts</name>
  <files>
    src/components/providers/lenis-provider.tsx
    src/lib/animations.ts
    src/app/layout.tsx
  </files>
  <action>
1. **src/lib/animations.ts** — Varianti condivise per consistenza visiva:

```typescript
export const EASE_SMOOTH = [0.25, 0.1, 0.25, 1] as const
export const EASE_OUT_EXPO = [0.19, 1, 0.22, 1] as const

export const DURATION = {
  fast: 0.3,
  normal: 0.6,
  slow: 1.0,
  hero: 1.4,
} as const

export const staggerContainer = {
  hidden: {},
  visible: {
    transition: { staggerChildren: 0.1, delayChildren: 0.2 },
  },
}

export const fadeInUp = {
  hidden: { opacity: 0, y: 40 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: DURATION.normal, ease: EASE_SMOOTH },
  },
}

export const fadeIn = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { duration: DURATION.normal, ease: EASE_SMOOTH },
  },
}

export const scaleIn = {
  hidden: { opacity: 0, scale: 0.95 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { duration: DURATION.normal, ease: EASE_SMOOTH },
  },
}
```

2. **src/components/providers/lenis-provider.tsx** — Lenis con GSAP ticker sync. CRITICO: `autoRaf: false` perche GSAP ticker guida il loop. Segui ESATTAMENTE il pattern dalla research:

```typescript
"use client"

import { createContext, useContext, useEffect, useState } from "react"
import Lenis from "lenis"
import gsap from "gsap"
import { ScrollTrigger } from "gsap/ScrollTrigger"

gsap.registerPlugin(ScrollTrigger)

const LenisContext = createContext<Lenis | null>(null)

export function LenisProvider({ children }: { children: React.ReactNode }) {
  const [lenis, setLenis] = useState<Lenis | null>(null)

  useEffect(() => {
    const lenisInstance = new Lenis({
      autoRaf: false,
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
      syncTouch: false, // Better mobile perf
    })

    lenisInstance.on("scroll", ScrollTrigger.update)

    const update = (time: number) => {
      lenisInstance.raf(time * 1000) // GSAP gives seconds, Lenis wants ms
    }
    gsap.ticker.add(update)
    gsap.ticker.lagSmoothing(0)

    setLenis(lenisInstance)

    return () => {
      lenisInstance.off("scroll", ScrollTrigger.update)
      gsap.ticker.remove(update)
      lenisInstance.destroy()
    }
  }, [])

  return (
    <LenisContext.Provider value={lenis}>
      {children}
    </LenisContext.Provider>
  )
}

export function useLenis() {
  return useContext(LenisContext)
}
```

3. **src/app/layout.tsx** — Aggiorna il layout esistente (da Plan 01-01) per wrappare con LazyMotion e LenisProvider. L'ordine e: `<LazyMotion>` fuori, `<LenisProvider>` dentro. LazyMotion wrappa una volta sola cosi tutti i `m.div` funzionano senza wrapper individuale.

```typescript
// Aggiungi questi import:
import { LazyMotion, domAnimation } from "motion/react"
import { LenisProvider } from "@/components/providers/lenis-provider"

// Nel body, wrappa children:
<body className="font-body">
  <LazyMotion features={domAnimation}>
    <LenisProvider>
      {children}
    </LenisProvider>
  </LazyMotion>
</body>
```

IMPORTANTE: Mantieni TUTTO il resto del layout (font, metadata, className su html). Aggiungi SOLO i wrapper.
  </action>
  <verify>
    - `npm run dev` avvia senza errori
    - Lo scroll della pagina ha inerzia morbida (diversa dallo scroll nativo — si nota fermando la rotella/trackpad)
    - Browser console: nessun warning o errore da Lenis, GSAP, o Motion
    - Verifica in DevTools Network che `motion` non carica il bundle completo da 34kb
  </verify>
  <done>LenisProvider sincronizzato con GSAP ticker, LazyMotion con domAnimation wrappa l'app, lib/animations.ts esporta varianti condivise.</done>
</task>

<task type="auto">
  <name>Task 2: Animation primitives (FadeIn, SlideUp, StaggerContainer) + pagina demo</name>
  <files>
    src/components/animations/fade-in.tsx
    src/components/animations/slide-up.tsx
    src/components/animations/stagger-container.tsx
    src/app/page.tsx
  </files>
  <action>
1. **src/components/animations/fade-in.tsx** — Wrapper FadeIn con direzione configurabile. Usa `m.div` (NON `motion.div`). Importa easing da lib/animations.ts per consistenza:

```typescript
"use client"
import { m } from "motion/react"
import { EASE_SMOOTH, DURATION } from "@/lib/animations"

interface FadeInProps {
  children: React.ReactNode
  delay?: number
  direction?: "up" | "down" | "left" | "right" | "none"
  className?: string
}

export function FadeIn({ children, delay = 0, direction = "up", className }: FadeInProps) {
  const offsets = {
    up: { y: 40 },
    down: { y: -40 },
    left: { x: 40 },
    right: { x: -40 },
    none: {},
  }

  return (
    <m.div
      initial={{ opacity: 0, ...offsets[direction] }}
      whileInView={{ opacity: 1, x: 0, y: 0 }}
      viewport={{ once: true, margin: "-100px" }}
      transition={{ duration: DURATION.normal, delay, ease: EASE_SMOOTH as unknown as number[] }}
      className={className}
    >
      {children}
    </m.div>
  )
}
```

2. **src/components/animations/slide-up.tsx** — SlideUp specifico per reveal di testo/sezioni dal basso con distanza maggiore:

```typescript
"use client"
import { m } from "motion/react"
import { EASE_OUT_EXPO, DURATION } from "@/lib/animations"

interface SlideUpProps {
  children: React.ReactNode
  delay?: number
  className?: string
  distance?: number
}

export function SlideUp({ children, delay = 0, className, distance = 80 }: SlideUpProps) {
  return (
    <m.div
      initial={{ opacity: 0, y: distance }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-50px" }}
      transition={{ duration: DURATION.slow, delay, ease: EASE_OUT_EXPO as unknown as number[] }}
      className={className}
    >
      {children}
    </m.div>
  )
}
```

3. **src/components/animations/stagger-container.tsx** — Orchestratore stagger per i figli. I figli devono usare le varianti da lib/animations.ts (fadeInUp, fadeIn, scaleIn):

```typescript
"use client"
import { m } from "motion/react"
import { staggerContainer } from "@/lib/animations"

interface StaggerContainerProps {
  children: React.ReactNode
  className?: string
  delay?: number
}

export function StaggerContainer({ children, className, delay = 0 }: StaggerContainerProps) {
  return (
    <m.div
      variants={{
        ...staggerContainer,
        visible: {
          ...staggerContainer.visible,
          transition: {
            ...staggerContainer.visible.transition,
            delayChildren: delay,
          },
        },
      }}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, margin: "-50px" }}
      className={className}
    >
      {children}
    </m.div>
  )
}
```

4. **src/app/page.tsx** — SOSTITUISCI la pagina di test del design system con una demo scrollabile che mostra TUTTE le animazioni + il design system. La pagina deve essere lunga abbastanza da scrollare (minimo 300vh). Struttura:

   **Sezione 1 — Hero simulata** (100vh): Titolo "One More Ink" grande in font-display color gold con `<SlideUp>`, sottotitolo in bone-muted con `<FadeIn delay={0.3}>`

   **Sezione 2 — Color palette** (bg-ink-deep): Grid di card colorate che mostrano tutti i token. Ogni card wrappata in `<FadeIn>` con delay incrementale. Usa `<StaggerContainer>` per il grid.

   **Sezione 3 — Typography showcase** (bg-ink): Titoli, paragrafi, e testo in varie dimensioni con `<SlideUp>` su ogni blocco.

   **Sezione 4 — Animation demo** (bg-ink-surface): Dimostra esplicitamente FadeIn da tutte le direzioni (up, down, left, right, none), SlideUp con distanze diverse, e StaggerContainer con card figlie che usano varianti fadeInUp.

   **Sezione 5 — Anchor test**: Link interni `<a href="#sezione-2">` per verificare che Lenis gestisce gli anchor link.

   Ogni sezione deve avere un id (e.g., `id="color-palette"`) per il test degli anchor.

   I figli dentro StaggerContainer devono essere `<m.div variants={fadeInUp}>` per funzionare col sistema di varianti.
  </action>
  <verify>
    - La pagina e scrollabile e le animazioni si attivano quando le sezioni entrano nel viewport
    - FadeIn: gli elementi appaiono con fade da tutte e 4 le direzioni + "none"
    - SlideUp: gli elementi salgono dal basso con easing expo
    - StaggerContainer: le card figlie appaiono una dopo l'altra con delay incrementale
    - Anchor link (#sezione-2): cliccando, Lenis scrolla smoothly alla sezione
    - Back button del browser: funziona normalmente dopo la navigazione anchor
    - `npm run build` completa senza errori
  </verify>
  <done>I 3 componenti animazione (FadeIn, SlideUp, StaggerContainer) sono funzionanti, usano m.div con LazyMotion, importano easing/durate da lib/animations.ts, e la pagina demo li mostra tutti in azione.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Design system completo con dark theme, animazioni, e smooth scroll. La pagina demo mostra tutti i colori, la tipografia, e le animation primitives in azione.</what-built>
  <how-to-verify>
    1. Apri http://localhost:3000 nel browser
    2. **Dark theme**: Lo sfondo e scuro (#121212), il testo e caldo/bone, gli accenti sono gold — tutto leggibile
    3. **Font**: I titoli usano un serif (Cormorant Garamond), il body usa Inter
    4. **Smooth scroll**: Scrolla con la rotella — l'inerzia e morbida, diversa dallo scroll nativo
    5. **Animazioni**: Scrolla verso il basso — le sezioni appaiono con fade/slide quando entrano nel viewport
    6. **Stagger**: Nella sezione animation demo, le card appaiono una dopo l'altra
    7. **Anchor link**: Clicca il link alla sezione 2 — lo scroll e smooth verso la sezione
    8. **Responsive**: Riduci il browser a 320px — nessun overflow, tutto leggibile
    9. **Performance**: Apri DevTools > Performance > CPU throttling 4x, scrolla — nessun jank evidente
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run dev` avvia senza errori, pagina mostra dark theme con animazioni
2. Lenis smooth scroll attivo — inerzia morbida diversa da scroll nativo
3. FadeIn, SlideUp, StaggerContainer animano correttamente al viewport entry
4. Anchor links funzionano con smooth scroll Lenis
5. Back button non rotto
6. `npm run build` produce static export senza errori
7. Nessun jank visibile con CPU throttling 4x
8. Il bundle Motion usa LazyMotion (verificabile via Network tab — nessun chunk da 34kb)
</verification>

<success_criteria>
- LenisProvider con autoRaf:false sincronizzato a GSAP ticker
- LazyMotion con domAnimation wrappa l'app (bundle ~5kb, non 34kb)
- 3 animation primitives (FadeIn, SlideUp, StaggerContainer) funzionanti con m.div
- lib/animations.ts esporta varianti condivise usate dai componenti
- Pagina demo scrollabile dimostra tutte le animazioni + anchor links
- Human verification approvata
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
