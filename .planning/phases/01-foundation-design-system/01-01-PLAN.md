---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - one-more-ink/package.json
  - one-more-ink/next.config.ts
  - one-more-ink/tsconfig.json
  - one-more-ink/src/app/layout.tsx
  - one-more-ink/src/app/page.tsx
  - one-more-ink/src/app/globals.css
  - one-more-ink/src/lib/constants.ts
  - one-more-ink/src/lib/data.ts
autonomous: true

must_haves:
  truths:
    - "Il sito si avvia con npm run dev senza errori"
    - "La pagina mostra testo bone (#F5F0E8) su sfondo ink (#121212) — dark theme coerente"
    - "Le utility class Tailwind v4 (bg-ink, text-bone, text-gold) funzionano correttamente"
    - "I font display (serif) e body (sans-serif) si caricano con font-display swap senza flash su sfondo scuro"
    - "Il layout e responsive da 320px a 1440px+ senza overflow orizzontale"
  artifacts:
    - path: "one-more-ink/src/app/globals.css"
      provides: "Tailwind v4 @theme con OKLCH tokens dark"
      contains: "@theme"
    - path: "one-more-ink/src/app/layout.tsx"
      provides: "Root layout con font, metadata, dark body"
      exports: ["default"]
    - path: "one-more-ink/src/app/page.tsx"
      provides: "Pagina di test che verifica tutti i token del design system"
      min_lines: 30
    - path: "one-more-ink/src/lib/constants.ts"
      provides: "Costanti brand: contatti, social links, orari"
      exports: ["STUDIO_INFO", "SOCIAL_LINKS", "BUSINESS_HOURS"]
    - path: "one-more-ink/src/lib/data.ts"
      provides: "Scaffold dati statici artisti e servizi"
      exports: ["artists", "services"]
    - path: "one-more-ink/next.config.ts"
      provides: "Config static export"
      contains: "output.*export"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals"
    - from: "src/app/layout.tsx"
      to: "next/font"
      via: "Font loading"
      pattern: "next/font"
---

<objective>
Scaffold del progetto Next.js 16 con Tailwind v4, design system dark con token OKLCH verificati per accessibilita, typography con next/font, e dati statici dello studio.

Purpose: Stabilire le fondamenta su cui ogni componente e pagina futura viene costruito — colori, font, spacing, dati.
Output: Progetto Next.js funzionante con dark theme visibile, font caricati, utility Tailwind operative, dati statici pronti.
</objective>

<execution_context>
@/Users/federicobavieri/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicobavieri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16, installa dipendenze, configura static export</name>
  <files>
    one-more-ink/ (intero scaffold)
    one-more-ink/next.config.ts
    one-more-ink/package.json
  </files>
  <action>
1. Crea il progetto Next.js 16 nella root del repo (il repo IS il progetto, non creare una sottocartella):

```bash
cd /Users/federicobavieri/Desktop/Claude\ Code/one-more-ink
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias
```

Se create-next-app non funziona nella directory corrente (perche ha .git/.planning), crea in una temp e sposta i file. L'importante e che i file Next.js (src/, package.json, next.config.ts, etc.) siano nella ROOT del repo, NON in una sottocartella.

2. Installa le dipendenze di Phase 1:

```bash
npm install motion gsap @gsap/react lenis
```

3. Configura `next.config.ts` per static export:

```typescript
import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  output: "export",
  images: {
    unoptimized: true, // Switch to next-image-export-optimizer in Phase 4
  },
}

export default nextConfig
```

4. Verifica che `npm run dev` parte senza errori.
  </action>
  <verify>
    - `npm run dev` avvia il server senza errori
    - `npm run build` completa con static export (cartella `out/`)
    - `package.json` contiene motion, gsap, @gsap/react, lenis nelle dependencies
  </verify>
  <done>Il progetto Next.js 16 e scaffoldato nella root del repo, con static export configurato e tutte le dipendenze Phase 1 installate.</done>
</task>

<task type="auto">
  <name>Task 2: Design system dark — globals.css con @theme OKLCH, font, layout.tsx, page di test</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/app/page.tsx
    src/lib/constants.ts
    src/lib/data.ts
  </files>
  <action>
1. **globals.css** — Sostituisci il contenuto generato da create-next-app con il design system dark. Usa ESATTAMENTE i token OKLCH dalla research:

```css
@import "tailwindcss";
@import "lenis/dist/lenis.css";

@theme {
  /* Backgrounds - dark gray, NOT pure black (prevents halation) */
  --color-ink: oklch(0.18 0.000 90);
  --color-ink-deep: oklch(0.17 0.000 90);
  --color-ink-surface: oklch(0.22 0.000 90);

  /* Gold accents - ALL pass WCAG 4.5:1 on ink */
  --color-gold: oklch(0.77 0.139 91);
  --color-gold-muted: oklch(0.75 0.085 82);
  --color-gold-light: oklch(0.80 0.106 85);

  /* Text - warm whites */
  --color-bone: oklch(0.96 0.012 80);
  --color-bone-muted: oklch(0.91 0.023 85);

  /* Typography */
  --font-display: var(--font-display-face), serif;
  --font-body: var(--font-body-face), sans-serif;

  /* Spacing rhythm */
  --spacing-section: 6rem;
  --spacing-section-mobile: 4rem;
}

body {
  @apply bg-ink text-bone;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

:focus-visible {
  outline: 2px solid var(--color-gold);
  outline-offset: 2px;
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

2. **layout.tsx** — Root layout con font loading via next/font. Scegli:
   - Display font: **Cormorant Garamond** (premium serif, ottimi italic, feel tattoo/editorial) — da Google Fonts via next/font/google
   - Body font: **Inter** (massima leggibilita su schermi, variable font) — da next/font/google

   Imposta le CSS custom properties `--font-display-face` e `--font-body-face` sul tag html/body cosi che @theme li risolve. Metadata in italiano con titolo "One More Ink — Tattoo & Piercing Studio Modena". Lang="it".

```typescript
import type { Metadata } from "next"
import { Cormorant_Garamond, Inter } from "next/font/google"
import "./globals.css"

const displayFont = Cormorant_Garamond({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  style: ["normal", "italic"],
  variable: "--font-display-face",
  display: "swap",
})

const bodyFont = Inter({
  subsets: ["latin"],
  variable: "--font-body-face",
  display: "swap",
})

export const metadata: Metadata = {
  title: "One More Ink — Tattoo & Piercing Studio Modena",
  description: "Studio di tatuaggi e piercing a Modena. Federica Morselli e Stefano Mazzotta: traditional, blackwork, fine line, ornamental, neo-traditional, realismo.",
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="it" className={`${displayFont.variable} ${bodyFont.variable}`}>
      <body className="font-body">
        {children}
      </body>
    </html>
  )
}
```

3. **page.tsx** — Pagina di test design system. NON e una pagina finale, e un test visivo che mostra:
   - Tutti i colori background (ink, ink-deep, ink-surface)
   - Tutti i colori testo (bone, bone-muted, gold, gold-muted, gold-light)
   - Typography display + body a varie dimensioni
   - Un check visivo dei contrasti
   - Layout responsive (flex/grid che si adatta da mobile a desktop)

   Includi header "Design System — One More Ink" in font display, sezioni colorate, e un paio di paragrafi di testo per verificare leggibilita. Usa Tailwind utility classes (bg-ink, text-bone, text-gold, font-display, font-body).

4. **src/lib/constants.ts** — Costanti dello studio:

```typescript
export const STUDIO_INFO = {
  name: "One More Ink",
  tagline: "Tattoo & Piercing Studio",
  address: "Via Nobili 20/22, 41126 Modena",
  phone: "+39 327 0991523",
  whatsappUrl: "https://wa.me/393270991523",
  googleMapsUrl: "", // da aggiungere in Phase 5
} as const

export const SOCIAL_LINKS = {
  instagram: {
    url: "https://www.instagram.com/onemoreink/",
    handle: "@onemoreink",
  },
  facebook: {
    url: "https://www.facebook.com/ONEmoreINK/",
    name: "ONE more INK",
  },
} as const

export const BUSINESS_HOURS = [
  { day: "Lunedi", hours: "Chiuso" },
  { day: "Martedi", hours: "13:00 – 19:30" },
  { day: "Mercoledi", hours: "13:00 – 19:30" },
  { day: "Giovedi", hours: "13:00 – 19:30" },
  { day: "Venerdi", hours: "13:00 – 19:30" },
  { day: "Sabato", hours: "10:30 – 18:00" },
  { day: "Domenica", hours: "Chiuso" },
] as const
```

5. **src/lib/data.ts** — Scaffold dati statici (popolati minimamente, espansi in fasi successive):

```typescript
export interface Artist {
  id: string
  name: string
  slug: string
  instagram: string
  instagramUrl: string
  specialties: string[]
  bio: string
  yearsExperience: number
}

export interface TattooStyle {
  id: string
  name: string
  slug: string
  description: string
}

export const artists: Artist[] = [
  {
    id: "federica",
    name: "Federica Morselli",
    slug: "federica-morselli",
    instagram: "@unpiccolofioccodineve",
    instagramUrl: "https://www.instagram.com/unpiccolofioccodineve/",
    specialties: ["Traditional", "Blackwork", "Fine Line", "Ornamental", "Color"],
    bio: "13 anni di esperienza. Specializzata in stili che spaziano dal traditional al fine line, con un occhio particolare per l'ornamental e il colore.",
    yearsExperience: 13,
  },
  {
    id: "stefano",
    name: "Stefano Mazzotta",
    slug: "stefano-mazzotta",
    instagram: "@ste49arts",
    instagramUrl: "https://www.instagram.com/ste49arts/",
    specialties: ["Blackwork", "Neo-Traditional", "Realismo"],
    bio: "Artista con forte identita visiva. Blackwork potente, neo-traditional curato nel dettaglio, realismo d'impatto.",
    yearsExperience: 0, // da verificare
  },
]

export const tattooStyles: TattooStyle[] = [
  { id: "traditional", name: "Traditional", slug: "traditional", description: "Linee spesse, colori saturi, iconografia classica." },
  { id: "neo-traditional", name: "Neo-Traditional", slug: "neo-traditional", description: "Evoluzione del traditional con dettagli e sfumature moderne." },
  { id: "blackwork", name: "Blackwork", slug: "blackwork", description: "Nero pieno, pattern geometrici, contrasti forti." },
  { id: "fine-line", name: "Fine Line", slug: "fine-line", description: "Linee sottili e delicate, dettagli minuziosi." },
  { id: "ornamental", name: "Ornamental", slug: "ornamental", description: "Pattern decorativi, mandala, simmetrie." },
  { id: "realismo", name: "Realismo", slug: "realismo", description: "Riproduzione fotografica, ombre e luci naturali." },
]
```
  </action>
  <verify>
    - `npm run dev` mostra la pagina di test con dark theme visibile
    - Apri browser DevTools: verifica che `bg-ink` applica il background corretto (circa #121212)
    - Verifica che `text-bone`, `text-gold` applicano i colori corretti
    - Verifica che il font display (Cormorant Garamond) si carica sui titoli e Inter sul body
    - Ridimensiona il browser da 320px a 1440px: nessun overflow orizzontale
    - `npm run build` completa senza errori
    - Importa `{ STUDIO_INFO }` e `{ artists }` da console/test: dati presenti
  </verify>
  <done>Il design system dark e visibile e funzionante: token OKLCH in globals.css generano utility Tailwind, font display e body si caricano con swap, la pagina di test mostra tutti i colori e la tipografia, constants e data sono pronti per l'uso nelle fasi successive.</done>
</task>

</tasks>

<verification>
1. `npm run dev` avvia senza errori e la pagina mostra dark theme con testo leggibile
2. Le utility Tailwind v4 custom (bg-ink, bg-ink-deep, bg-ink-surface, text-bone, text-bone-muted, text-gold, text-gold-muted, text-gold-light, font-display, font-body) sono tutte funzionanti
3. `npm run build` produce static export nella cartella `out/`
4. Il layout e responsive senza overflow orizzontale da 320px a 1440px+
5. I font si caricano con swap (nessun flash di testo invisibile su sfondo scuro)
</verification>

<success_criteria>
- Progetto Next.js 16 nella root con static export configurato
- Dark theme OKLCH con tutti i contrasti WCAG 4.5:1+ verificati
- Font Cormorant Garamond (display) e Inter (body) caricati con next/font
- Dati statici studio (constants.ts, data.ts) pronti per componenti futuri
- Build completa senza errori
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
